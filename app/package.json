{
  "name": "app",
  "version": "1.0.0",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "dev": "node --env-file=.env.dev --watch src/main.mjs",
    "test": "vitest run"
  },
  "packageManager": "yarn@4.9.4",
  "dependencies": {
    "fastify": "5.5.0"
  },
  "devDependencies": {
    "vitest": "4.0.6"
  },
  "engines": {
    "node": ">=22"
  }
}
